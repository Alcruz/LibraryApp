@model LibraryApp.Models.BookView

@{
    ViewBag.Title = "Eliminar foto";
}

<h2>Eliminar foto</h2>

<h3>¿Está seguro de eliminar esta foto?</h3>

@using (Html.BeginForm("DeletePhoto", "Books", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Libros</h4>

        <img src="@Url.Content(Model.Photo)" alt="Image" style="width:150px;height:3000px;max-width: 150%; height: auto;" />

        <hr />

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ISBN)
        @Html.HiddenFor(model => model.BookId)
        @Html.HiddenFor(model => model.BookTypeId)
        @Html.HiddenFor(model => model.DateOfRelease)
        @Html.HiddenFor(model => model.Edition)
        @Html.HiddenFor(model => model.EditorialId)
        @Html.HiddenFor(model => model.WriterId)
        @Html.HiddenFor(model => model.Plot)
        @Html.HiddenFor(model => model.Photo)
        @Html.HiddenFor(model => model.ISBN)
        @Html.HiddenFor(model => model.Title)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Eliminar foto" class="btn btn-danger" />
                @Html.ActionLink("Editar", "Edit", new { id = Model.WriterId }, new { @class = "btn btn-warning" })
                @Html.ActionLink("Regresar a la lista", "Index", new { }, new { @class = "btn btn-success" })
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
